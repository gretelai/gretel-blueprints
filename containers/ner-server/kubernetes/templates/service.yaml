apiVersion: v1
kind: Service
metadata:
  name: {{ include "gretel-ner.fullname" . }}
  labels:
    {{- include "gretel-ner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gretel-ner.selectorLabels" . | nindent 4 }}
