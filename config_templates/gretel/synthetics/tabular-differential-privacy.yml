# Default configuration for the Gretel Tabular DP model to generate synthetic
# data with differential privacy guarantees.

schema_version: "1.0"
name: "tabular-differential-privacy"
models:
  - tabular_dp:
      data_source: __tmp__
      params:
        epsilon: 1
        delta: 0.0001
        infer_domain: True
